<nav class="w-full border-b border-slate-800 bg-slate-950/50 sticky top-0 z-10 backdrop-blur">
  <div class="max-w-xl mx-auto px-2 overflow-x-auto scrollbar-hide">
    <div class="flex items-center">
      <button
        type="button"
        role="tab"
        aria-selected="true"
        data-tabs-target="tab"
        data-tab-id="pedir-frete"
        data-action="click->tabs#select"
        class="px-3 sm:px-4 py-3 text-xs sm:text-sm font-semibold border-b-2 border-emerald-500 text-emerald-300 transition-colors whitespace-nowrap"
      >
        🚚 Pedir frete
      </button>
      <button
        type="button"
        role="tab"
        aria-selected="false"
        data-tabs-target="tab"
        data-tab-id="como-funciona"
        data-action="click->tabs#select"
        class="px-3 sm:px-4 py-3 text-xs sm:text-sm font-medium border-b-2 border-transparent text-slate-400 hover:text-slate-200 transition-colors whitespace-nowrap"
      >
        ❓ Como funciona
      </button>
      <button
        type="button"
        role="tab"
        aria-selected="false"
        data-tabs-target="tab"
        data-tab-id="motoristas"
        data-action="click->tabs#select"
        class="px-3 sm:px-4 py-3 text-xs sm:text-sm font-medium border-b-2 border-transparent text-slate-400 hover:text-slate-200 transition-colors whitespace-nowrap"
      >
        👤 Motoristas
      </button>
    </div>
  </div>
</nav>
